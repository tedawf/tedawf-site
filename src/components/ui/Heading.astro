---
const headingOptions = {
  h1: "text-2xl",
  h2: "text-xl",
  h3: "text-lg",
  h4: "text-md",
  h5: "text-sm",
  h6: "text-xs",
};

type Props = {
  tagName: keyof typeof headingOptions;
  size: keyof typeof headingOptions;
};

const { tagName, size } = Astro.props;
const Tag = tagName;
---

<Tag class:list={["leading-tight", headingOptions[size]]}>
  <slot />
</Tag>
