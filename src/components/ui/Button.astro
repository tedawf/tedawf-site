---
import { cn } from "@/lib/utils";
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"button"> {
}

const { ...rest } = Astro.props;
---

<button
  class:list={cn(
    "shadow-light aria-pressed:shadow-inset",
    Astro.props.class
  )}
  {...rest}>
  <slot />
</button>
